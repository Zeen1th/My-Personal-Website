# Certificates

Drop certificate images here. The site already references:

- `ine-data-science.png` — your INE "Jr. Data Science Certificate" (the one you shared).
- `ibm-ai-analyst.png` — IBM Artificial Intelligence Analyst (optional, when you have it).

The certificate cards (next to the University of Hail entry) show a small thumbnail and
the "View certificate" link opens the full image. If a file is missing, the card just
shows a seal icon instead — nothing breaks.
